import React from 'react';
import { ThemeColorSchemeKey } from '../../theme';
import { Placement } from '../../types';
export interface MenuButtonProps {
    boundaryElement?: HTMLElement;
    button: React.ReactElement;
    id: string;
    menu?: React.ReactElement;
    placement?: Placement;
    popoverScheme?: ThemeColorSchemeKey;
    popoverRadius?: number | number[];
    /**
     * @beta Do not use in production.
     */
    portal?: boolean;
}
export declare function MenuButton({ boundaryElement, button: buttonProp, id, menu: menuProp, placement, popoverScheme, portal, popoverRadius, }: MenuButtonProps): JSX.Element;
//# sourceMappingURL=menuButton.d.ts.map